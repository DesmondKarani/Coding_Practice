#include <stdio.h>
int integer_add(int x, int y) {
	int result;
	result = x + y;
	return result;
}
int main(void)
{
	int sum;
	sum = integer_add;
	printf(Enter the x please:");
	scanf("%d",x);
	printf("enter y please:");
	scanf("%d", y);

	printf("the sum of 4 and 6 is %d\n", sum);
	return(0);
}
